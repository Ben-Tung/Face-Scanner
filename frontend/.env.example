# Frontend environment variables. Copy to .env.local for local (non-Docker)
# runs; docker-compose supplies these directly.
#
# NEXT_PUBLIC_* values are inlined into the browser bundle — never put a secret
# behind this prefix.

NEXT_PUBLIC_API_URL=http://localhost:8000
